name: hydrosar-on-demand
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.8
  - pip
  # For running
  - asf_search>=0.4
  - astropy
  - fiona
  - gdal>=3.2.0
  - hyp3_sdk>=1.1
  - ipywidgets=7.7.3          # must match OSL base env!
  - jupyterlab_widgets=1.1.2  # must match OSL base env!
  - jupyter
  - kernda
  - numpy
  - pysheds
  - rasterio
  - scikit-fuzzy
  - scikit-image
  - shapely
